{
    "name": "hamlet-monorepo",
    "version": "0.1.0",
    "private": true,
    "engines": {
        "node": ">=20.0.0",
        "npm": ">=9.0.0"
    },
    "workspaces": [
        "packages/*",
        "app/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "dev": "npm run dev --workspaces"
    }
}