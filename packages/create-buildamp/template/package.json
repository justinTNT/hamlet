{
    "name": "buildamp-app",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npm run build --workspaces",
        "dev": "npm run dev --workspaces",
        "dev:server": "hamlet serve",
        "generate": "hamlet gen",
        "generate:watch": "hamlet watch",
        "dev:web": "cd web && vite",
        "dev:backend": "cd server && node --watch server.js"
    },
    "devDependencies": {
        "concurrently": "^8.0.0",
        "hamlet-cli": "*"
    },
    "workspaces": [
        "web",
        "server"
    ]
}